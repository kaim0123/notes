import Link from "next/link";
import type { Metadata } from "next";
import {
  DocsPage,
  Hero,
  Eyebrow,
  Lead,
  Term,
  Heading,
  DocsFooter,
  Card,
  CardGrid,
  CardNumber,
  Analogy,
  Steps,
  RelatedList,
  RelatedLink,
} from "@/components/docs";

export const metadata: Metadata = {
  title: "イベント駆動アーキテクチャ",
};

export default function Page() {
  return (
    <DocsPage>
      <Hero>
        <Eyebrow>設計 &middot; アーキテクチャ 2000年代前半</Eyebrow>
        <h1>イベント駆動アーキテクチャ ― 「起きたこと」を非同期に伝える</h1>
        <Lead>
          システム同士が同期的な呼び出し(APIをリクエストして応答を待つ)で繋がっていると、呼び出し先が遅い・落ちているだけで全体が引きずられ、密結合になります。2000年代前半に広まった<Term>イベント駆動アーキテクチャ</Term>は、「何かが起きた」という事実(イベント)を非同期に配信することで、システム同士を疎結合にします。
        </Lead>
      </Hero>

      <Heading num="01">開始イベントから派生イベントへ ― 処理全体の流れ</Heading>
      <p>1つのイベントフローは、そのフロー全体のきっかけとなる<Term>開始イベント</Term>がイベントブローカーの特定のイベントチャネルに送信されるところから始まります。</p>
      <Steps>
        <li><strong>開始イベントの送信</strong> ― クライアントやサービスが「注文が作成された」のような開始イベントを、イベントブローカーの該当チャネルに送信する。</li>
        <li><strong>イベントプロセッサーが受信</strong> ― そのチャネルを購読しているイベントプロセッサーが開始イベントを受け取り、処理を開始する。</li>
        <li><strong>特定のタスクを実行</strong> ― イベントプロセッサーは、そのイベントの処理に関連する自分の担当分だけを実行する(在庫引当、与信チェックなど)。</li>
        <li><strong>派生イベントの発行</strong> ― 処理が終わると、非同期的に「自分が何をしたか」を派生イベントとしてイベントブローカーに通知する。呼び出し元に直接応答を返すわけではない。</li>
        <li><strong>次のプロセッサーが応答</strong> ― 別のイベントプロセッサーがその派生イベントに応答し、自分の処理を行い、さらに新しい派生イベントを発行する。これが業務フローの終端まで連鎖する。</li>
      </Steps>
      <p>この一連の流れの特徴は、どのプロセッサーも「次に誰が何をするか」を知らずに自分の仕事だけに集中できる点です。全体のオーケストレーションは(メディエーター型を使わない限り)存在せず、各イベントへの反応の連鎖として業務フローが結果的に形作られます。</p>

      <Heading num="02">イベントとメッセージの違い</Heading>
      <p><Term>イベント</Term>は「注文が作成された」のような、すでに起きた事実の通知です。発行者は誰が受け取るか、受け取った後どう処理されるかを知りませんし、応答も期待しません。一方<Term>メッセージ</Term>は「在庫を減らして」のような、特定の受信者に処理を依頼する命令で、発行者は受信者を意識しています。この違いが、疎結合の度合いを大きく左右します。</p>

      <Heading num="03">ブローカーのフェデレーションとクラスタ</Heading>
      <p>イベントブローカーのコンポーネントは、単一の巨大な1台として存在するのではなく、通常<Term>フェデレーション</Term>されています。つまり、ドメインごとに分かれた複数のブローカーインスタンス群を持ち、フェデレーションされたそれぞれのブローカーが、その特定のドメインのイベントフローで使われるすべてのイベントチャネルを抱えています(注文ドメイン用のブローカー、配送ドメイン用のブローカー、というように)。このアーキテクチャスタイルの性質上、チャネルの実装にはトピック・トピックエクスチェンジ・ストリームが使われます。</p>
      <table>
        <thead>
          <tr><th>用語</th><th>意味</th></tr>
        </thead>
        <tbody>
          <tr><td className="hl">インスタンス</td><td>実際に起動している1つのブローカープロセス。1台のマシン(またはコンテナ)上で動く実体。</td></tr>
          <tr><td className="hl">クラスタ</td><td>複数のインスタンスをひとまとめにし、外部からは1つの論理的なブローカーとして見せる集合。可用性(1台が落ちても残りが応答する)とスループット(負荷を複数インスタンスに分散する)の両方を得るための構成。</td></tr>
          <tr><td className="hl">フェデレーション</td><td>クラスタ(またはインスタンス)をドメイン単位で分割したもの。ドメインごとに独立したブローカー群を持つことで、あるドメインのイベント量やスキーマ変更が他ドメインに影響しない。</td></tr>
        </tbody>
      </table>

      <Heading num="04">ブロードキャストと非同期処理</Heading>
      <p>イベントは1人の受信者だけでなく、複数の購読者に同時に配信する<Term>ブロードキャスト機構</Term>(パブリッシュ/サブスクライブ)で配られることが多く、発行者は誰が購読しているかを知る必要がありません。処理も<Term>非同期処理機構</Term>(キューやメッセージブローカー)を介して行われるため、発行者は受信側の処理が終わるのを待たずに次の仕事に進めます。この「待たない」性質から、イベント駆動は<Term>応答性(レスポンシブネス)</Term>に優れます。呼び出し元はイベントを送った時点で自分の処理を終えられるため、体感速度が上がります。一方で、業務フロー全体が完了するまでの実時間(スループット)は、ブローカーを経由するホップの数や、各プロセッサーの処理速度に左右されるため、「応答が速い」ことと「全体が速く終わる」ことは別の指標として意識する必要があります。</p>

      <Heading num="05">イベントペイロードの設計</Heading>
      <p>イベントに載せるデータの量には、大きく2つの方針があります。</p>
      <table>
        <thead>
          <tr><th>種類</th><th>載せる内容</th><th>トレードオフ</th></tr>
        </thead>
        <tbody>
          <tr><td className="hl">データイベントペイロード</td><td>変更後の詳細データそのものを含める</td><td>受信側は再取得なしで処理できるが、ペイロードが大きくなり、時間差で内容が陳腐化(古くなる)するリスクを負う</td></tr>
          <tr><td className="hl">キーイベントペイロード</td><td>対象のID(キー)だけを含める</td><td>ペイロードは軽量で最新性の問題も起きにくいが、受信側は別途APIやDBへ問い合わせて詳細データを取得する手間と、その問い合わせ先への結合が発生する</td></tr>
        </tbody>
      </table>
      <p>どちらを選ぶかは、イベントの発生頻度・ペイロードのサイズ・受信側がその場でどこまでの情報を必要とするかによって決まります。</p>

      <Heading num="06">派生イベントと「ブヨの群れ」アンチパターン</Heading>
      <p>あるイベントの処理が、さらに新しいイベントを生む(<Term>派生イベント</Term>)ことがあります。「注文作成」イベントが「在庫引当」イベントを生み、それが「出荷準備」イベントを生む、というように連鎖していきます。この連鎖が制御なく広がると、1つの業務トランザクションが無数の小さなイベントに分裂し、何が起きているか誰にも追えなくなります。これは<Term>ブヨの群れ(a swarm of gnats)アンチパターン</Term>と呼ばれ、イベント駆動の代表的な落とし穴です。</p>

      <Analogy label="💡 たとえるなら">
        イベント駆動は、掲示板に「◯◯が起きました」と貼り紙をするようなものです。貼り紙をした人は、誰がそれを読むか、読んだ人が何をするかを知りません。これに対しメッセージは、特定の人に直接「これをやってください」と手紙を渡すようなものです。掲示板の貼り紙が貼り紙を呼び、次々に増えていく状態が「ブヨの群れ」です。
      </Analogy>

      <Heading num="07">エラー処理 ― ワークフロープロセッサーへの委譲</Heading>
      <p>イベントコンシューマーがメッセージキュー(イベントチャネル)経由でデータを受け取り処理している最中にエラーに遭遇したとき、その場で延々とリトライして止まるのではなく、エラーを<Term>ワークフロープロセッサー</Term>に委譲し、自分はキュー内の次のメッセージの処理へ進みます。ワークフロープロセッサーは委譲されたエラーを受け取ると、そのメッセージの何が問題だったのか(データ形式の不備か、業務ルール違反か、下流サービスが一時的に応答不能なだけか)を切り分け、リトライ・人手への通知・デッドレターキューへの退避などを判断します。こうしてエラー処理を本流の処理から切り離すことで、1件の異常なメッセージが他の正常なメッセージの処理まで止めてしまう事態を防ぎます。</p>

      <Heading num="08">データロスを防ぐ配信の仕組み</Heading>
      <p>非同期処理は「メッセージが確かに届いたか」を目視で確認しづらいため、<Term>AMQP</Term>(Advanced Message Queuing Protocol、RabbitMQなどが実装)や、Amazon SNSのような仕組みを使って、イベントの送信と到達確認を行います。典型的な流れは次の通りです。イベントはまず<Term>エクスチェンジ</Term>に発行され、エクスチェンジは、コンシューマー側のイベントプロセッサーがあらかじめ設定した<Term>バインディングルール</Term>に従って、そのイベントを購読している各イベントプロセッサー専用のキューへ転送します。</p>

      <Heading num="09">データロスが起こりうる3つの瞬間</Heading>
      <p>非同期処理は「送ったつもり」が「届いていない」に化けるリスクを常に抱えています。イベントプロセッサーA(発行側)とイベントプロセッサーB(受信側)の間で、典型的には次の3つの瞬間にデータロスが起こり得ます。</p>
      <Steps>
        <li><strong>①送信の失敗</strong> ― イベントプロセッサーAが発行したメッセージがキューに届かない。あるいは届いたとしても、イベントプロセッサーBがそのメッセージを取得する前にブローカー自体がクラッシュしてしまう。</li>
        <li><strong>②取得後・処理前のクラッシュ</strong> ― イベントプロセッサーBがキューから次に処理するメッセージを取得した後、そのイベントを実際に処理する前にクラッシュしてしまう。</li>
        <li><strong>③永続化の失敗</strong> ― イベントプロセッサーBは処理自体を完了させたが、何らかのデータエラーが原因で、その結果をデータベースに永続化できない。</li>
      </Steps>
      <p>いずれも、非同期通信では「送信・取得・処理・永続化」が1つのアトミックな操作として保証されないために起こる、中途半端な状態が原因です。</p>

      <Heading num="10">データロスを防ぐ3つのイベント転送パターン</Heading>
      <p>上記のようなデータロスに対処するため、実務では主に次の3つのパターンが使われます。</p>
      <table>
        <thead>
          <tr><th>パターン</th><th>仕組み</th></tr>
        </thead>
        <tbody>
          <tr><td className="hl">永続化されたメッセージ＋同期送信</td><td>イベントは処理の高速化のためにメモリに保存されるだけでなく、物理的なデータストアにも永続化される。ブローカーがイベントをデータベースへ永続化したという応答を受け取るまで、送信側は次のイベントを送らない(同期送信)ことで、ブローカーが復旧すればイベントを処理できる状態を保つ。</td></tr>
          <tr><td className="hl">クライアント確認応答モード</td><td>イベントにクライアントIDを紐付けたうえでキューに残し続け、他のコンシューマーがそのイベントを重複して読み取ったり処理したりできないようにする。処理を完了した合図として確認応答(ACK)を返すまでメッセージはキューから削除されないため、イベントプロセッサーBが処理中にクラッシュしても、メッセージが失われることはない。</td></tr>
          <tr><td className="hl">DBコミットによるACIDトランザクション</td><td>イベントプロセッサーがデータベースコミットを発行して初めて、データが永続化されたことが保証される。Last Participant Support(LPS)を使えば、処理が完了しイベントが永続化されたことを確認してから、永続化キューから該当イベントを削除できるため、コミットと削除の間で取りこぼしが起こらない。</td></tr>
        </tbody>
      </table>

      <Heading num="11">リクエスト・リプライ処理 ― 非同期の中に擬似的な同期を作る</Heading>
      <p>イベント駆動は基本的に応答を期待しない非同期通信ですが、呼び出し元がその場で結果を必要とする場面もあります。このときは<Term>擬似同期通信</Term>という手法で、非同期の仕組みの上に同期的な体験を組み立てます。具体的には、リクエストとなるイベントに一意の<Term>相関ID(Correlation ID)</Term>を付与して送信し、応答専用に用意した<Term>テンポラリーキュー</Term>(一時キュー)で、その相関IDと一致する応答イベントの到着を待ちます。呼び出し元は完全にブロックするのではなく、タイムアウトを設定したうえで該当の相関IDを持つ応答を待ち、時間内に来なければタイムアウトエラーとして扱います。</p>

      <Heading num="12">メディエーター型 vs ブローカー型</Heading>
      <p>複数のイベントにまたがる業務フロー全体の順序や整合性を管理したい場合は、中央の調整役を置く<Term>メディエーター型イベント駆動アーキテクチャ</Term>を使います。中央の調整役を置かず、各サービスがイベントを見て自律的に次の処理を判断する形は<Term>ブローカー型</Term>と呼ばれ、よりシンプルですが全体の流れを一箇所で把握しにくくなります。メディエーター型では、中央に置かれたイベントメディエーターが開始イベントを受け取り、業務フローに含まれる各ステップの呼び出し順序や条件分岐を把握したうえで、対応するイベントプロセッサーを順番に(あるいは並行して)呼び出します。これを<Term>メディエータートポロジー</Term>と呼びます。個々のイベントプロセッサーは自分の担当処理を実行して結果をメディエーターに返すだけでよく、次に誰が呼ばれるかを知る必要はありません。全体の順序や整合性の管理責任がメディエーター1箇所に集約されるため業務フローの可視性は高まりますが、メディエーター自体が単一障害点やボトルネックになりやすいというトレードオフがあります。</p>

      <Heading num="13">データトポロジー ― イベントプロセッサーとデータベースの結びつき方</Heading>
      <p>イベントプロセッサーがデータベースとどう結びつくかにも、結合度の異なる3つのパターンがあります。</p>
      <table>
        <thead>
          <tr><th>トポロジー</th><th>特徴</th></tr>
        </thead>
        <tbody>
          <tr><td className="hl">モノリシックデータベーストポロジー</td><td>すべてのイベントプロセッサーが1つの共有データベースを参照・更新する。実装はシンプルだが、あるプロセッサーのスキーマ変更が他のプロセッサーに影響し、疎結合の恩恵を打ち消してしまう。</td></tr>
          <tr><td className="hl">ドメインデータベーストポロジー</td><td>関連するイベントプロセッサー群をドメイン単位でグループ化し、ドメインごとに専用のデータベースを持たせる。モノリシックほど密結合ではないが、同一ドメイン内のプロセッサー間ではまだデータベースを介した結合が残る。</td></tr>
          <tr><td className="hl">専用データベーストポロジー</td><td>イベントプロセッサー1つひとつが自分専用のデータベースを持つ。結合は最も弱くマイクロサービス的な自律性が得られる一方、複数のイベントプロセッサーにまたがるデータの整合性を取るための工夫(<Link href="/database/distributed-transactions/saga">Saga</Link>や<Link href="/database/distributed-transactions/tcc">TCC</Link>など)が別途必要になる。</td></tr>
        </tbody>
      </table>

      <Heading num="14">リクエストベースモデルとイベントベースモデル、どちらを選ぶか</Heading>
      <p>最後に、同期的な<Term>リクエストベースモデル</Term>(リクエスト/レスポンス)と、これまで見てきた<Term>イベントベースモデル</Term>のトレードオフを整理します。</p>
      <table>
        <thead>
          <tr><th>モデル</th><th>メリット</th><th>デメリット</th></tr>
        </thead>
        <tbody>
          <tr><td className="hl">リクエストベース</td><td>処理順序や状態が追いやすく、エラー処理の見通しが良い。呼び出しに応じた結果をその場で返せる。</td><td>呼び出し元と呼び出し先が密結合になりやすく、呼び出し先の障害や遅延がそのまま呼び出し元に伝播する。</td></tr>
          <tr><td className="hl">イベントベース</td><td>発行側と購読側が疎結合になり、新しい購読者の追加も容易。スケーラビリティや障害分離に優れる。</td><td>業務フロー全体の流れが見えにくくなり、デバッグや監査が難しい。「ブヨの群れ」のように無秩序に連鎖が広がるリスクがある。</td></tr>
        </tbody>
      </table>
      <p>実際のシステムでは、即時性が求められる箇所はリクエストベース、疎結合や非同期処理が有効な箇所はイベントベースというように、両者を併用するハイブリッドな設計が一般的です。</p>

      <Heading num="15">実装イメージ ― AWSで組むとこうなる</Heading>
      <p>ここまでの概念は抽象的な話でしたが、AWSでは次のように具体的なサービスへ対応します。</p>
      <table>
        <thead>
          <tr><th>概念</th><th>AWSでの実体</th></tr>
        </thead>
        <tbody>
          <tr><td className="hl">イベントブローカー</td><td>SNSトピック(＋後段のSQS)</td></tr>
          <tr><td className="hl">開始イベントの送信</td><td><code>sns.publish()</code>の呼び出し</td></tr>
          <tr><td className="hl">イベントプロセッサー</td><td>SQSをトリガーに自動起動するLambda関数</td></tr>
          <tr><td className="hl">派生イベントの発行</td><td>処理後、別のSNSトピックへ再度<code>publish()</code>する</td></tr>
          <tr><td className="hl">購読</td><td>SNS Subscription(SQS・Lambda・HTTPなどを紐付ける設定)</td></tr>
        </tbody>
      </table>
      <p>例えば「注文が確定した」という開始イベントを1回<code>publish()</code>するだけで、メール送信・在庫更新・出荷準備という3つのイベントプロセッサー(Lambda)が、互いの存在を知らないまま同時に起動します。発行側のコードに受信側の関数名が一切登場しないという点が、疎結合を実装レベルで支えています。具体的なコード例と構成図は<Link href="/cloud/aws/integration/sns">SNS</Link>のページを参照してください。</p>

      <p>次のページでは、大量アクセスに耐えるためにデータベースを経由せずメモリ上でデータを処理する<Link href="/design/architecture/sys/space-based">スペースベースアーキテクチャ</Link>を見ていきます。</p>

      <CardGrid>
        <Card><CardNumber>1</CardNumber><h4>イベントは「事実」の通知</h4><p>応答を期待せず、誰が受け取るかを発行者は知らない。</p></Card>
        <Card><CardNumber>2</CardNumber><h4>非同期・ブロードキャストで疎結合に</h4><p>受信側の処理を待たず、複数の購読者へ同時配信できる。</p></Card>
        <Card><CardNumber>3</CardNumber><h4>派生イベントの連鎖に注意</h4><p>制御のない連鎖は「ブヨの群れ」を生み、全体の見通しを悪くする。</p></Card>
      </CardGrid>

      <DocsFooter
        related={
          <RelatedList>
                    <RelatedLink href="/design/architecture/sys/soa" tag="設計">オーケストレーション駆動SOA</RelatedLink>
                    <RelatedLink href="/design/architecture/sys/space-based" tag="設計">スペースベースアーキテクチャ</RelatedLink>
                    <RelatedLink href="/design/architecture/sys/microservices" tag="設計">マイクロサービスアーキテクチャ</RelatedLink>
                    <RelatedLink href="/database/distributed-transactions" tag="データベース">分散トランザクション</RelatedLink>
                  </RelatedList>
        }
      />
    </DocsPage>
  );
}
