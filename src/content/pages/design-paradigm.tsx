import Link from "next/link";
import type { Metadata } from "next";
import {
  DocsPage,
  Hero,
  Eyebrow,
  Lead,
  Term,
  Heading,
  DocsFooter,
  Card,
  CardGrid,
  CardNumber,
  Analogy,
  Aside,
  DiagramFrame,
  Timeline,
  TimelineItem,
  TimelineLabel,
} from "@/components/docs";

export const metadata: Metadata = {
  title: "パラダイム",
};

export default function Page() {
  return (
    <DocsPage>
      <Hero>
        <Eyebrow>設計</Eyebrow>
        <h1>パラダイム ― コードの「書き方」はどう変わってきたか</h1>
        <Lead>
          <Term>プログラミングパラダイム</Term>とは、「コードをどういう考え方で組み立てるか」という書き方の流派です。同じ問題でも、手順として書くか、オブジェクトとして書くか、関数の組み合わせとして書くかで、コードの形はまったく変わります。歴史を追うと、それぞれのパラダイムが「前の書き方で困っていたこと」への回答として生まれてきたことが分かります。
        </Lead>
      </Hero>

      <Heading num="01">設計の話は「粒度」で並べると見通しがよい</Heading>
      <p>
        設計と呼ばれる話題は幅が広く、「マイクロサービスにするか」も「変数名をcamelCaseにするか」も同じ<Term>設計</Term>という言葉で語られます。混乱を避けるには、扱っている<Term>粒度</Term>で並べ直すのが早道です。
      </p>

      <DiagramFrame
        slug="design-paradigm-granularity"
        aspect="640 / 330"
        caption="設計の話題を粒度順に並べた図。大きい方から、システム全体やアプリ内部の分け方を決めるアーキテクチャ、クラス数個の粒度で繰り返し現れる設計パターン、1つの関数・モジュールをどう書くかというパラダイム、1つの名前・1行の表記を揃えるコーディング規約、の4段になる。設計原則はこの4段すべてにまたがり、どの粒度でも共通して効いてくる判断基準として働く。"
      />

      <p>
        この<Term>設計</Term>セクションは、そのままこの並びで構成しています。まずこのページで一番小さい単位である「どう書くか」のパラダイムを押さえ、続いて<Link href="/design/principles">設計原則</Link>という判断基準、<Link href="/design/architecture">アーキテクチャ</Link>という全体の骨組み、<Link href="/design/patterns">設計パターン</Link>という部品の定石、最後に<Link href="/design/conventions">コーディング規約</Link>という表記の取り決めを見ていきます。
      </p>

      <Heading num="02">パラダイムは置き換わらず、積み重なる</Heading>
      <p>
        パラダイムには、他の設計項目と違って「新しいものが出たので古いものは使わない」という関係がほとんどありません。後から出てきたパラダイムは前のものを置き換えるのではなく、<Term>選択肢として積み重なって</Term>いきます。
      </p>

      <Timeline>
        <TimelineItem era="1950年代">機械語 / アセンブリ</TimelineItem>
        <TimelineItem era="1950-60年代">手続き型</TimelineItem>
        <TimelineItem era="1960-70年代">構造化</TimelineItem>
        <TimelineItem era="1970-90年代">オブジェクト指向</TimelineItem>
        <TimelineItem era="1980年代〜">関数型</TimelineItem>
      </Timeline>
      <TimelineLabel>
        パラダイムが実用として広まった時期の目安(理論の起源はいずれもこれより早い)。
      </TimelineLabel>

      <p>
        例外は<Term>機械語・アセンブリ</Term>で、これは高級言語の登場によって、通常のアプリケーション開発では直接書く機会がほぼ無くなった唯一の項目です。とはいえ完全に消えたわけではなく、OS・コンパイラ・組み込みなど、ハードウェアに近い領域では今も現役です。
      </p>

      <Heading num="03">それぞれ「前の困りごと」への回答として生まれた</Heading>
      <p>
        パラダイムを丸暗記しようとすると、似た用語が並んで区別がつかなくなります。<Term>どんな困りごとへの回答だったか</Term>という順で見ると、順番そのものに意味があることが分かります。
      </p>

      <DiagramFrame
        slug="design-paradigm-units"
        aspect="680 / 350"
        caption="4つのパラダイムを、前の書き方で困っていたことへの回答として並べた対応表。機械語では処理の流れが読めないという問題に手続き型(基本単位は手順=関数)が、GOTOが飛び交って流れを追えないという問題に構造化(基本単位は順次・分岐・反復)が、規模が大きくなるとデータと処理の対応が崩れるという問題にオブジェクト指向(基本単位はデータと処理をまとめたオブジェクト)が、状態変化と並行処理でバグの原因が読み切れないという問題に関数型(基本単位は純粋関数)が、それぞれ対応する。"
      />

      <Heading num="04">4つのパラダイムを個別に見る</Heading>

      <h3>手続き型 ― 手順をそのまま書く</h3>
      <p>
        <Term>手続き型</Term>は、やることを起きる順に並べ、まとまりを<Term>手続き(関数・サブルーチン)</Term>として切り出す書き方です。「上から順に読める」という強みは今も変わらず、スクリプトやバッチ処理では現在でもこれが最も素直な選択になります。基本単位は<Term>手順</Term>です。
      </p>

      <h3>構造化 ― 制御の流れを定型に絞る</h3>
      <p>
        初期の手続き型では、任意の行へ飛べる<code>GOTO</code>が多用され、実行経路が絡み合って読めなくなる問題(いわゆるスパゲッティコード)が起きました。<Term>構造化プログラミング</Term>は、制御の流れを<Term>順次・分岐・反復</Term>の3つだけに絞ることで、コードを上から下へ追えるようにしたパラダイムです。
      </p>
      <p>
        現代の言語では<code>if</code>・<code>while</code>・<code>for</code>・関数が最初から備わっており、構造化を「選ぶ」機会はほぼありません。すでに言語機能として吸収されているという意味で、最も成功したパラダイムとも言えます。
      </p>

      <h3>オブジェクト指向 ― データと処理をまとめる</h3>
      <p>
        規模が大きくなると、「このデータを触っていい処理はどれか」が追えなくなります。<Term>オブジェクト指向</Term>は、関連するデータとその操作を1つの<Term>オブジェクト</Term>にまとめ、外から触れる範囲を絞る(<Term>カプセル化</Term>)ことでこれに答えました。さらに<Term>継承</Term>・<Term>ポリモーフィズム</Term>によって、共通部分をまとめたり、呼び出す側を変えずに振る舞いだけを差し替えたりできます。
      </p>
      <p>
        業務ルールが多く、状態を持つドメインを扱う場面では今も主力です。一方で、継承を深く重ねると変更に弱くなるため、現代では<Term>継承より合成</Term>を優先するのが定石になっています。
      </p>

      <h3>関数型 ― 副作用を減らす</h3>
      <p>
        <Term>関数型</Term>は、同じ入力なら常に同じ出力を返し、外部の状態を変更しない<Term>純粋関数</Term>を基本単位に据えます。データを書き換えず新しい値を作る<Term>イミュータブル(不変)</Term>な扱いと組み合わせることで、「どこで値が変わったのか分からない」タイプのバグを構造的に減らせます。
      </p>
      <p>
        複数の処理を同時に進める並行処理とも相性が良く、共有データを書き換えないぶん、競合状態(レースコンディション)を心配する箇所が減ります。JavaScript/TypeScriptで<code>map</code>・<code>filter</code>・<code>reduce</code>を使ってループを書き換えるのも、この発想の一部です。
      </p>

      <table>
        <thead>
          <tr>
            <th>パラダイム</th>
            <th>基本単位</th>
            <th>得意なこと</th>
          </tr>
        </thead>
        <tbody>
          <tr>
            <td className="hl">手続き型</td>
            <td>手順(関数)</td>
            <td>処理の流れをそのまま記述できる。学習コストが低い</td>
          </tr>
          <tr>
            <td className="hl">構造化</td>
            <td>制御構造(順次・分岐・反復)</td>
            <td>実行経路を上から下へ追える形に保てる</td>
          </tr>
          <tr>
            <td className="hl">オブジェクト指向</td>
            <td>オブジェクト(データ+処理)</td>
            <td>状態を持つ大規模なドメインの整理・再利用</td>
          </tr>
          <tr>
            <td className="hl">関数型</td>
            <td>純粋関数</td>
            <td>副作用に由来するバグの抑制。並行処理との相性</td>
          </tr>
        </tbody>
      </table>

      <Heading num="05">実務ではマルチパラダイムで書く</Heading>
      <p>
        実際の開発で1つのパラダイムだけを使うことは稀です。JavaScript/TypeScriptのような<Term>マルチパラダイム</Term>言語では、モジュールの分け方はオブジェクト指向的に、データの加工は関数型的に、というように場所ごとに使い分けるのが普通です。
      </p>
      <Aside label="使い分けの目安">
        状態を持ち、業務ルールがまとわりつくもの(注文・在庫・ユーザー)はオブジェクトとしてまとめる。入力から出力を計算するだけのもの(集計・整形・変換)は純粋関数として書く。この2つを意識するだけでも、コードの置き場所の迷いはかなり減ります。
      </Aside>

      <Heading num="06">同じ発想が、粒度を変えて現れる</Heading>
      <p>
        パラダイムは「1つの関数・1つのモジュールをどう書くか」という粒度の話でした。面白いのは、まったく同じ発想が、より大きな粒度である<Link href="/design/architecture">アーキテクチャ</Link>や<Link href="/design/patterns">設計パターン</Link>にも現れることです。
      </p>

      <CardGrid>
        <Card>
          <h4>手続き型 → 一方向に流す</h4>
          <p>
            上から下へ順に処理する発想は、データを段階的に変換していくパイプラインアーキテクチャや、業務処理を1本の手続きとして書くTransaction Scriptに対応します。
          </p>
        </Card>
        <Card>
          <h4>構造化 → 決まった経路だけ通す</h4>
          <p>
            飛び越えを許さない発想は、隣接する層としか通信しないレイヤードアーキテクチャや、処理を数珠つなぎにするミドルウェアチェーンに対応します。
          </p>
        </Card>
        <Card>
          <h4>オブジェクト指向 → 境界でまとめる</h4>
          <p>
            データと処理を1単位にまとめる発想は、サービス単位でデータごと独立させるマイクロサービスや、Strategy・Decoratorといったパターンに対応します。
          </p>
        </Card>
      </CardGrid>

      <p>
        関数型の「副作用を境界の外へ押し出す」発想も同じで、起きた出来事を不変なイベントとして流すイベント駆動アーキテクチャや、読み取りと書き込みを分けるCQRSに対応します。
      </p>

      <Analogy label="💡 たとえるなら">
        パラダイムが「1文・1段落をどう書くか」という文章作法だとすれば、アーキテクチャや設計パターンは、その作法で書かれた段落を章・本としてどう束ねるかという編集方針です。手順として書かれた段落は手順書のような章立てに、役割ごとに書かれた段落は役割ごとの独立した章にまとまりやすい ―
        同じ発想が、粒度を変えて繰り返し現れています。
      </Analogy>

      <Heading num="まとめ">押さえておきたい3点</Heading>
      <CardGrid>
        <Card>
          <CardNumber>1</CardNumber>
          <h4>回答の連鎖として覚える</h4>
          <p>
            各パラダイムは、前の書き方で困っていたことへの回答。順番そのものに意味があります。
          </p>
        </Card>
        <Card>
          <CardNumber>2</CardNumber>
          <h4>基本単位が違うだけ</h4>
          <p>
            手順・制御構造・オブジェクト・純粋関数。何を1単位と見るかが、そのまま書き方の違いになります。
          </p>
        </Card>
        <Card>
          <CardNumber>3</CardNumber>
          <h4>選ぶのではなく使い分ける</h4>
          <p>
            現代の言語はマルチパラダイム。状態を持つ部分と計算だけの部分で書き方を変えるのが実務的です。
          </p>
        </Card>
      </CardGrid>

      <p>
        次は、どのパラダイムで書くにしても共通して効いてくる判断基準 ―
        <Link href="/design/principles">設計原則</Link>を見ていきます。
      </p>

      <DocsFooter href="/design/paradigm" />
    </DocsPage>
  );
}
