import Link from "next/link";
import type { Metadata } from "next";
import {
  DocsPage,
  Hero,
  Eyebrow,
  Lead,
  Term,
  Heading,
  DiagramFrame,
  Card,
  CardGrid,
  CardNumber,
  Analogy,
  Aside,
  DocsFooter,
} from "@/components/docs";

export const metadata: Metadata = { title: "認証・認可" };

export default function Page() {
  return (
    <DocsPage>
      <Hero>
        <Eyebrow>セキュリティ</Eyebrow>
        <h1>認証・認可 ― 誰かを確かめ、何をしてよいかを決める</h1>
        <Lead>
          <Link href="/security/crypto">暗号技術</Link>で、通信の相手が本物かを確かめる手立てまで来ました。残るのは、その通信の向こうにいる<strong>人</strong>のほうです。ここには2つの別々の判定があります ― <Term>認証</Term>(あなたは誰か)と<Term>認可</Term>(あなたは何をしてよいか)。混同されがちですが、担当する場所も、失敗したときの返し方も違います。実装の手順は<Link href="/backend/auth">認証の実装</Link>が受け持つので、このページは<strong>何をどう判定しているのか</strong>の側を扱います。
        </Lead>
      </Hero>

      <Heading num="01">2つの判定は、順番も役割も違う</Heading>
      <p>
        リクエストは2つの関門を順に通ります。先に「名乗りが本物か」を確かめ(認証)、それが済んでから「その人にこの操作を許すか」を判定する(認可)。この順序は入れ替えられません。<Term>誰か分からないうちは、何をしてよいかも決めようがない</Term>からです。
      </p>

      <DiagramFrame
        slug="security-auth-authn-authz"
        aspect="640 / 330"
        caption="リクエストが通る2つの関門。認証で名乗りが確かめられなければ401を返し、確かめられたら認可へ進む。認可で権限が足りなければ403を返し、両方を通ってはじめて処理が実行される。401は「名乗り直してほしい」、403は「名乗りは受け取ったが、それはさせられない」という意味で、応答の違いがそのまま役割の違いにあたる。"
      />

      <table>
        <thead>
          <tr><th></th><th>認証(Authentication)</th><th>認可(Authorization)</th></tr>
        </thead>
        <tbody>
          <tr><td className="hl">問い</td><td>あなたは誰か</td><td>あなたは何をしてよいか</td></tr>
          <tr><td className="hl">判定の材料</td><td>パスワード、端末、生体</td><td>役割、所有関係、契約プラン</td></tr>
          <tr><td className="hl">回数</td><td>ふつうログイン時に1回</td><td>リクエストごとに毎回</td></tr>
          <tr><td className="hl">失敗時</td><td>401 Unauthorized</td><td>403 Forbidden</td></tr>
        </tbody>
      </table>

      <p>
        表で見ておきたいのは<strong>回数の行</strong>です。認証は入口で1回ですが、認可はリクエストのたびに要ります。「ログインしている人だけがアクセスできる画面」を作ったつもりでも、他人のIDを指定したURLでその人のデータが返るなら、認可が抜けています。<Term>ログイン済みであることは、そのデータを見てよい理由にならない</Term> ― この抜けは動作テストでは見つからず、正常系がすべて通ったまま残ります。認可の詳細な設計は<Link href="/security/authz">認可</Link>で扱います。
      </p>

      <Heading num="02">何をもって本人とみなすか</Heading>
      <p>
        認証の材料は、性質で3つに分けられます。これを<Term>認証の3要素</Term>と呼びます。
      </p>

      <DiagramFrame
        slug="security-auth-factors"
        aspect="640 / 340"
        caption="認証の3要素と多要素認証の関係。知識(パスワード・PIN)は盗み見や使い回しに弱く、所持(スマホ・ICカード)は紛失や盗難に弱く、生体(指紋・顔)は漏れても取り替えられないという弱点を持つ。種類の違うものを2つ以上組み合わせたものが多要素認証で、片方が破られてももう片方が残る。パスワードと秘密の質問はどちらも知識なので、2つ重ねても要素は1つのまま。"
      />

      <p>
        3要素は強さの順位ではなく<Term>破られ方の種類</Term>です。だから多要素認証の条件は「2つ以上」ではなく<strong>「別の種類を2つ以上」</strong>になります。パスワードと秘密の質問はどちらも知識なので、同じ破られ方 ― 漏えいしたリストからの推測 ― でまとめて抜かれます。
      </p>
      <p>
        生体には他の2つに無い性質があります。<Term>変更できない</Term>ことです。パスワードは漏れたら変えられ、カードは再発行できますが、指紋は取り替えられません。だから生体認証は、生体情報そのものをサーバーへ送って照合するのではなく、端末の中で照合して<strong>「照合できた」という署名だけを送る</strong>形が主流になりました。パスキーがこの形で、鍵は端末を出ず、サーバー側に漏れて困る秘密が残りません。認証方式そのものの変遷は<Link href="/security/identity">認証プロトコルの変遷</Link>で扱います。
      </p>

      <Aside label="多要素でも突破される道はある">
        <Link href="/security/attacks">攻撃手法</Link>で見たとおり、確認コードを電話で読み上げさせる手口や、承認通知を何度も送りつけて押させる手口があります。要素を増やすことは万能薬ではなく、<strong>攻撃コストを引き上げる措置</strong>です。通知に「どこからのログインか」を表示する、コードに宛先を紐づけるといった、人が判断を誤りにくい作りとセットにします。
      </Aside>

      <Heading num="03">パスワードをどう預かるか</Heading>
      <p>
        パスワード認証を採るなら、避けて通れないのが保存の仕方です。原則は1つ ― <strong>元に戻せる形で持たない</strong>。ここで<Link href="/security/crypto">暗号技術</Link>の区別が効いてきます。暗号化は鍵があれば元に戻せる双方向の変換で、ハッシュは戻せない一方向の変換です。パスワードは<Term>戻せてしまうこと自体がリスク</Term>なので、ハッシュで持ちます。
      </p>

      <table>
        <thead>
          <tr><th>保存方式</th><th>DBが漏れたときに起きること</th><th>可否</th></tr>
        </thead>
        <tbody>
          <tr><td className="hl">平文</td><td>全ユーザーのパスワードがそのまま流出する</td><td>NG</td></tr>
          <tr><td className="hl">暗号化(復号可能)</td><td>鍵も一緒に漏れれば、すべて元に戻される</td><td>NG</td></tr>
          <tr><td className="hl">汎用ハッシュのみ</td><td>戻せはしないが、まとめ解読と使い回しの発見を許す</td><td>不足</td></tr>
          <tr><td className="hl">ソルト付き・計算コスト付きハッシュ</td><td>1件ずつ総当たりし直すしかない</td><td>OK</td></tr>
        </tbody>
      </table>

      <p>
        3行目が落とし穴です。ハッシュにしただけでは足りません。同じパスワードからは同じ値が出るので、値が一致する行を探すだけで<strong>使い回している人がまとめて分かり、1件解ければ全員解けます</strong>。
      </p>

      <DiagramFrame
        slug="security-auth-password-storage"
        aspect="760 / 322"
        caption="ソルトの有無で、漏洩後の結果がどう変わるか。左のソルトなしでは、同じパスワードを使う3人がすべて同じハッシュ値になり、1件解ければ同じ値の全員が解ける。右はユーザーごとに違うソルトが混ざるため、同じパスワードでも別々の値になり、あらかじめ作った対応表が効かず1件ずつやり直しになる。ストレッチングはさらに1件あたりの計算を重くする。"
      />

      <p>
        必要なのは2つの上乗せです。<Term>ソルト</Term>はユーザーごとに異なるランダム値を混ぜ、同じパスワードでも別の値になるようにします。これで、あらかじめ計算しておいた対応表(レインボーテーブル)も、まとめ解読も効かなくなります。<Term>ストレッチング</Term>は計算を意図的に重くする措置で、ログインは1回きりなので体感は変わりませんが、何億回も試す総当たりには何億倍で効きます。<strong>bcryptやArgon2はこの2つを最初から備えており、SHA-256を素で使うのは「この2つが無い」という意味</strong>になります。
      </p>

      <Analogy label="💡 たとえるなら">
        ハッシュは絵の具を混ぜてしまうことに似ています。混ざった色から元の色は取り出せませんが、同じ手順でもう一度混ぜて色が一致するかは確かめられます。サーバーはパスワードを覚えているのではなく、毎回混ぜ直して色を見比べているだけです。ソルトは、人ごとに違う下地の色を最初に置いておくようなものです。
      </Analogy>

      <Heading num="04">ログインの周りは、穴が集まる場所</Heading>
      <p>
        パスワードの保存を正しくしても、その周辺の作りで台無しになることがあります。ログイン画面は<strong>認証されていない相手が正当に叩ける、数少ない入口</strong>なので、ここだけは攻撃者も遠慮なく試せます。
      </p>

      <table>
        <thead>
          <tr><th>落とし穴</th><th>何が起きるか</th><th>直し方</th></tr>
        </thead>
        <tbody>
          <tr><td className="hl">エラーの出し分け</td><td>「IDが存在しません」でIDの実在を判定できる(ユーザー列挙)</td><td>どちらの失敗も同じ文言・同じ応答時間にする</td></tr>
          <tr><td className="hl">試行回数が無制限</td><td>時間さえかければ総当たりが通る</td><td>IDごと・送信元ごと両方で失敗数を数え、間隔を空けさせる</td></tr>
          <tr><td className="hl">ログイン後もIDを引き継ぐ</td><td>攻撃者が用意したセッションのまま昇格される(セッション固定化)</td><td>認証成功時にセッションIDを再発行する</td></tr>
          <tr><td className="hl">ログアウトが画面上だけ</td><td>サーバー側の状態が残り、盗まれたIDが使い続けられる</td><td>サーバー側のセッションを確実に破棄する</td></tr>
          <tr><td className="hl">パスワード回復が弱い</td><td>本人確認の弱い経路が、そのまま裏口になる</td><td>回復フローを本体の認証と同じ強さで設計する</td></tr>
        </tbody>
      </table>

      <p>
        最後の行は見落とされがちです。<Term>回復フローは、認証のもう1つの入口</Term>です。生年月日と登録メールだけでパスワードを再設定できるなら、多要素認証を必須にしていても、そちらを通れば同じ結果になります。強い扉の隣に、弱い勝手口を作らないという話です。
      </p>

      <Heading num="05">一度確かめた結果を、どう持ち回るか</Heading>
      <p>
        HTTPは1回ごとに完結する通信なので、認証が済んでも次のリクエストではまた「誰?」から始まります。そこで、確かめた結果を持ち回る仕組みが要ります。方式は大きく2つで、<strong>状態をサーバーに置くか、証明書のような紙をクライアントに持たせるか</strong>の違いです。
      </p>

      <table>
        <thead>
          <tr><th></th><th>セッション方式</th><th>トークン方式</th></tr>
        </thead>
        <tbody>
          <tr><td className="hl">持ち回るもの</td><td>サーバー側の状態を指すID</td><td>署名された主張そのもの</td></tr>
          <tr><td className="hl">検証</td><td>サーバーが自分の記録を引く</td><td>署名を検証すれば記録を引かなくてよい</td></tr>
          <tr><td className="hl">取り消し</td><td>サーバー側で消せば即座に無効</td><td>有効期限が切れるまで生き続ける</td></tr>
        </tbody>
      </table>

      <p>
        トークンが速いのは記録を引かないからですが、<Term>引かない仕組みは取り消せない</Term>という代償が付いてきます。この取り消しの難しさをどう運用で埋めるかは<Link href="/backend/auth-token">トークンの運用</Link>が、自前で認証を持たず外部の事業者に委ねる形は<Link href="/backend/auth-oauth">外部IdP連携(OAuth 2.0 / OIDC)</Link>が扱います。セッションIDとCookieの属性設計は<Link href="/security/session">セッションとCookie</Link>、同じ言葉が指す複数のものの整理は<Link href="/security/token">トークンの全体像</Link>で扱います。
      </p>

      <Heading num="まとめ">入口は2つあり、判定も2つある</Heading>
      <CardGrid>
        <Card>
          <CardNumber>1</CardNumber>
          <h4>認証は1回、認可は毎回</h4>
          <p>ログイン済みであることは、そのデータを見てよい理由にならない。認可の抜けは正常系では見つからない。</p>
        </Card>
        <Card>
          <CardNumber>2</CardNumber>
          <h4>多要素の条件は「別の種類」</h4>
          <p>3要素は強さの順位ではなく破られ方の種類。同じ種類を重ねても同じ手口でまとめて抜かれる。</p>
        </Card>
        <Card>
          <CardNumber>3</CardNumber>
          <h4>戻せる形で預からない</h4>
          <p>ソルトと計算コストを備えたハッシュで保存する。回復フローも同じ強さで設計する。</p>
        </Card>
      </CardGrid>

      <DocsFooter href="/security/auth" />
    </DocsPage>
  );
}
