import Link from "next/link";
import type { Metadata } from "next";
import {
  DocsPage,
  Hero,
  Eyebrow,
  Lead,
  Term,
  Heading,
  DiagramFrame,
  Card,
  CardGrid,
  CardNumber,
  Analogy,
  Aside,
  DocsFooter,
} from "@/components/docs";

export const metadata: Metadata = { title: "認可" };

export default function Page() {
  return (
    <DocsPage>
      <Hero>
        <Eyebrow>セキュリティ</Eyebrow>
        <h1>認可 ― ログイン済みは、見てよい理由にならない</h1>
        <Lead>
          <Link href="/security/auth">認証・認可</Link>で、2つは別の判定だと見ました。認証は入口で1回、認可はリクエストのたびに毎回 ― この非対称さが、認可を実装しにくくしています。しかも<strong>抜けていても画面は正しく動きます</strong>。自分のIDでしか操作しない限り、不具合として現れないからです。ここでは、その抜けがどう生まれ、どう塞ぐのかを見ます。
        </Lead>
      </Hero>

      <Heading num="01">判定は、信頼境界の内側に置く</Heading>
      <p>
        認可の実装で最初に決めるのは、<Term>判定をどちら側に置くか</Term>です。ブラウザに送られたコードとデータは、すべて利用者が読めて、書き換えられます。そこに置いた判定は、判定ではなく表示の工夫にしかなりません。
      </p>

      <DiagramFrame
        slug="security-authz-boundary"
        aspect="760 / 300"
        caption="認可の判定を置く場所を、信頼境界の線で示したもの。左のブラウザ側に置けるのは、管理者向けボタンを表示するかどうかといった見た目の制御だけで、開発者ツールやAPIの直接呼び出しで迂回できる。右のサーバー側が信用できる唯一の場所で、誰であるか、その行の持ち主か、その役割に許された操作かという判定はすべてこちらに置き、リクエストのたびに実行する。"
      />

      <p>
        よくある誤解が「管理者用のボタンを一般利用者には表示しない」で対策したつもりになることです。ボタンを隠しても、そのボタンが叩くAPIは生きています。<strong>ブラウザを使わずにリクエストを送れば、画面の制御は丸ごと迂回できます</strong>。
      </p>

      <Analogy label="💡 たとえるなら">
        認証が「入館証を見せて建物に入る」ことなら、認可は「入ったあと、どの部屋のドアが開くか」です。案内板から社長室の表記を消しても、ドアに鍵がかかっていなければ誰でも入れます。消すべきは表記ではなく、開ける権利のほうです。
      </Analogy>

      <Heading num="02">IDOR ― 番号を1つ変えるだけの事故</Heading>
      <p>
        認可不備の典型が<Term>IDOR(Insecure Direct Object Reference)</Term>です。自分の注文詳細が <code>/orders/123</code> だったとして、数字を <code>124</code> に変えたら他人の注文が表示された ― という形で現れます。
      </p>

      <DiagramFrame
        slug="security-authz-idor"
        aspect="760 / 300"
        caption="確認する内容の違いによるIDORの発生。ログイン済みかどうかだけを確認する実装では、自分の注文も他人の注文も200で返ってしまう。その行の持ち主が本当にこの利用者かまで確認する実装では、他人の注文は403で拒否される。この事故は正常系のテストでは見つからない ― 自分のIDでしか操作しない限り、画面は最後まで正しく動く。"
      />

      <p>
        原因ははっきりしています。サーバーが<strong>「ログイン済みか」だけを見て、「そのリソースの持ち主か」を見ていない</strong>。認証は通っているので、ログにもエラーは残りません。連番のIDを使っていれば、他人のデータを端から取得することさえできます。
      </p>

      <Aside label="IDを推測しにくくすることは、対策ではない">
        連番をUUIDに変えれば当てにくくはなりますが、これは<Term>隠すことによる安全</Term>にすぎません。URLは共有され、ログに残り、参照元ヘッダに載り、他人の目に触れる経路がいくつもあります。<strong>「見つかりにくい」と「権限のない人には渡さない」はまったく別</strong>で、後者だけが対策です。推測しにくいIDは、対策の代わりではなく上乗せとして使います。
      </Aside>

      <Heading num="03">許可リストで書き、既定は拒否にする</Heading>
      <p>
        認可のルールは「誰が・何に対して・何をしてよいか」の形で明文化し、すべての入口に一貫して適用します。ここでの設計方針が<Term>デフォルト拒否</Term>です。
      </p>

      <DiagramFrame
        slug="security-authz-default-deny"
        aspect="700 / 300"
        caption="禁止リスト方式と許可リスト方式で、ルールを書き忘れたときにどちらへ倒れるか。禁止リストでは、書き忘れたエンドポイントが誰でも通れる状態になり、気づくのは外部から悪用されたあとになる。許可リストでは、書き忘れたエンドポイントは自動的に拒否され、気づくのは担当者から動かないという苦情が来たとき。書き忘れはどちらでも起きるので、選んでいるのは倒れる方向のほう。"
      />

      <p>
        判定に使うデータも、必ず<strong>サーバー側が持っているもの</strong>から引きます。<code>hidden</code>フィールドの <code>role=admin</code> や、Cookieに入れた権限情報を信用してはいけません。「この利用者の役割は何か」「この行の所有者は誰か」は、その都度データベースに問い合わせて確かめます。
      </p>

      <Heading num="04">ルールをどう表すか</Heading>
      <p>
        「誰が・何を」の表し方には、いくつか定番の形があります。規模と変更の頻度で選びます。
      </p>

      <table>
        <thead>
          <tr><th>モデル</th><th>判定の根拠</th><th>向く場面</th></tr>
        </thead>
        <tbody>
          <tr><td className="hl">所有ベース</td><td>そのデータの持ち主が本人か</td><td>利用者が自分のデータだけを扱うサービス</td></tr>
          <tr><td className="hl">ロールベース(RBAC)</td><td>利用者に与えた役割(管理者・編集者・閲覧者)</td><td>役割の種類が少なく、安定している組織向け</td></tr>
          <tr><td className="hl">属性ベース(ABAC)</td><td>利用者・対象・状況の属性を組み合わせた条件</td><td>部署や時間帯など、条件が複雑に絡む場合</td></tr>
        </tbody>
      </table>

      <p>
        実際のアプリでは、この3つが混ざります ― 役割で大枠を分け、その中で所有者かどうかを見る、という形が多くなります。混ざること自体は問題ありませんが、<Term>判定を書く場所が散らばると、抜けが生まれます</Term>。層に分けた設計であれば、認可の判定を1か所に集めておくと、レビューでも追いやすくなります。層の分け方は<Link href="/backend/layers">アプリケーションの組み立て</Link>を参照してください。
      </p>

      <Heading num="05">抜けをどう見つけるか</Heading>
      <p>
        認可の抜けは正常系のテストでは見つかりません。<strong>他人のIDで叩くテストを、明示的に書く</strong>必要があります。
      </p>

      <ul>
        <li><strong>他人のリソースを指すテスト</strong> ― Aでログインし、Bのリソースを取得・更新・削除しようとして403になることを確かめる。</li>
        <li><strong>権限のない役割で叩くテスト</strong> ― 一般利用者のトークンで管理者向けエンドポイントを叩き、拒否されることを確かめる。</li>
        <li><strong>入口の棚卸し</strong> ― 追加したエンドポイントに認可が付いているかを、一覧で突き合わせる。</li>
      </ul>

      <p>
        テストの書き方そのものは<Link href="/test/api">APIのテスト</Link>と<Link href="/test/security">セキュリティテスト</Link>で扱っています。ここで言いたいのは、<Term>認可のテストは正常系の裏返しではない</Term>ということです。書かなければ、そこは永久に検査されません。
      </p>

      <Heading num="まとめ">毎回・サーバーで・許可リストで</Heading>
      <CardGrid>
        <Card>
          <CardNumber>1</CardNumber>
          <h4>判定は信頼境界の内側</h4>
          <p>ボタンを隠すのは親切。判定がブラウザ側にあるうちは、実装されていないのと同じ。</p>
        </Card>
        <Card>
          <CardNumber>2</CardNumber>
          <h4>持ち主かどうかを毎回</h4>
          <p>ログイン済みは、そのデータを見てよい理由にならない。IDは書き換えられる前提で扱う。</p>
        </Card>
        <Card>
          <CardNumber>3</CardNumber>
          <h4>書き忘れは拒否へ倒す</h4>
          <p>許可リストなら、抜けは「動かない」として現れる。禁止リストでは「開いている」として現れる。</p>
        </Card>
      </CardGrid>

      <DocsFooter href="/security/authz" />
    </DocsPage>
  );
}
