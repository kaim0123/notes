import Link from "next/link";
import type { Metadata } from "next";
import {
  DocsPage, Hero, Eyebrow, Lead, Term, Heading, DocsFooter,
  Card, CardGrid, CardNumber, Analogy, Aside, Steps, DiagramFrame,
} from "@/components/docs";

export const metadata: Metadata = { title: "イベント駆動アーキテクチャ" };

export default function Page() {
  return (
    <DocsPage>
      <Hero>
        <Eyebrow>設計</Eyebrow>
        <h1>イベント駆動アーキテクチャ ― 起きたことを非同期に伝える</h1>
        <Lead>
          システム同士が同期的な呼び出しで繋がっていると、呼び出し先が遅い・落ちているだけで全体が引きずられ、密結合になります。2000年代前半に広まった<Term>イベント駆動アーキテクチャ</Term>は、「何かが起きた」という事実(イベント)を非同期に配信することで、システム同士を疎結合にします。
        </Lead>
      </Hero>

      <Heading num="01">開始イベントから派生イベントへ</Heading>
      <p>
        1つのイベントフローは、そのフロー全体のきっかけとなる<Term>開始イベント</Term>が、イベントブローカーの特定のチャネルに送信されるところから始まります。
      </p>
      <Steps>
        <li>クライアントやサービスが「注文が作成された」のような開始イベントを、ブローカーの該当チャネルへ送信する</li>
        <li>そのチャネルを購読しているイベントプロセッサーが開始イベントを受け取り、処理を開始する</li>
        <li>イベントプロセッサーは、自分の担当分だけを実行する(在庫引当、与信チェックなど)</li>
        <li>処理が終わると「自分が何をしたか」を派生イベントとしてブローカーへ通知する。呼び出し元に直接応答を返すわけではない</li>
        <li>別のイベントプロセッサーがその派生イベントに応答し、さらに新しい派生イベントを発行する。これが終端まで連鎖する</li>
      </Steps>
      <p>
        特徴は、どのプロセッサーも「次に誰が何をするか」を知らずに自分の仕事だけに集中できる点です。全体の業務フローは、各イベントへの反応の連鎖として結果的に形作られます。
      </p>

      <Heading num="02">イベントとメッセージの違い</Heading>
      <p>
        <Term>イベント</Term>は「注文が作成された」のような、すでに起きた事実の通知です。発行者は誰が受け取るか、受け取った後どう処理されるかを知りませんし、応答も期待しません。一方<Term>メッセージ</Term>は「在庫を減らして」のような、特定の受信者に処理を依頼する命令で、発行者は受信者を意識しています。この違いが、疎結合の度合いを大きく左右します。
      </p>

      <Heading num="03">ブロードキャストと非同期処理</Heading>
      <p>
        イベントは1人の受信者だけでなく、複数の購読者に同時に配信する<Term>パブリッシュ/サブスクライブ</Term>で配られることが多く、発行者は誰が購読しているかを知る必要がありません。処理もキューを介して行われるため、発行者は受信側の処理完了を待たずに次へ進めます。
      </p>
      <Aside label="応答が速いことと、全体が速く終わることは別">
        呼び出し元はイベントを送った時点で自分の処理を終えられるため、体感速度は上がります。しかし業務フロー全体が完了するまでの実時間は、ブローカーを経由するホップの数や各プロセッサーの処理速度に左右されます。この2つは別の指標として分けて意識する必要があります。
      </Aside>

      <Heading num="04">イベントペイロードの設計</Heading>
      <table>
        <thead>
          <tr><th>種類</th><th>載せる内容</th><th>トレードオフ</th></tr>
        </thead>
        <tbody>
          <tr>
            <td className="hl">データイベント</td>
            <td>変更後の詳細データそのものを含める</td>
            <td>受信側は再取得なしで処理できるが、ペイロードが大きくなり、時間差で内容が古くなるリスクを負う</td>
          </tr>
          <tr>
            <td className="hl">キーイベント</td>
            <td>対象のID(キー)だけを含める</td>
            <td>軽量で最新性の問題も起きにくいが、受信側は別途問い合わせが必要で、その問い合わせ先への結合が生じる</td>
          </tr>
        </tbody>
      </table>

      <Heading num="05">派生イベントと「ブヨの群れ」</Heading>
      <p>
        あるイベントの処理が、さらに新しいイベントを生むことがあります。「注文作成」が「在庫引当」を生み、それが「出荷準備」を生む、というように連鎖していきます。この連鎖が制御なく広がると、1つの業務トランザクションが無数の小さなイベントに分裂し、何が起きているか誰にも追えなくなります。これは<Term>ブヨの群れ(a swarm of gnats)アンチパターン</Term>と呼ばれ、イベント駆動の代表的な落とし穴です。
      </p>

      <Analogy label="💡 たとえるなら">
        イベント駆動は、掲示板に「◯◯が起きました」と貼り紙をするようなものです。貼った人は、誰がそれを読むか、読んだ人が何をするかを知りません。これに対しメッセージは、特定の人に直接「これをやってください」と手紙を渡すようなものです。貼り紙が貼り紙を呼び、次々に増えていく状態が「ブヨの群れ」です。
      </Analogy>

      <Heading num="06">メディエーター型とブローカー型</Heading>

      <DiagramFrame
        slug="design-architecture-event-driven-topologies"
        aspect="700 / 320"
        caption="ブローカー型とメディエーター型の比較。左のブローカー型では、ブローカーに発行されたイベントに在庫・決済・通知が自律的に反応し、処理後にそれぞれが派生イベントを発行する。疎結合だが全体の流れを1箇所で把握しにくい。右のメディエーター型では、中央のイベントメディエーターが順序と条件分岐を握り、各プロセッサーを順に呼び出して結果を受け取る。流れは追いやすいが、中央が単一障害点になりやすい。"
      />

      <p>
        中央の調整役を置かず、各サービスがイベントを見て自律的に次を判断する形が<Term>ブローカー型</Term>です。シンプルですが全体の流れを一箇所で把握しにくくなります。複数のイベントにまたがる業務フロー全体の順序や整合性を管理したい場合は、中央に<Term>イベントメディエーター</Term>を置きます。個々のプロセッサーは自分の処理を実行して結果を返すだけでよく、次に誰が呼ばれるかを知る必要がありません。可視性は高まりますが、メディエーター自体が単一障害点やボトルネックになりやすいというトレードオフがあります。
      </p>

      <Heading num="07">エラー処理 ― 本流から切り離す</Heading>
      <p>
        処理中にエラーへ遭遇したとき、その場で延々とリトライして止まるのではなく、エラーを<Term>ワークフロープロセッサー</Term>へ委譲し、自分はキュー内の次のメッセージへ進みます。ワークフロープロセッサーは、データ形式の不備か、業務ルール違反か、下流が一時的に応答不能なだけかを切り分け、リトライ・人手への通知・デッドレターキューへの退避などを判断します。こうしてエラー処理を本流から切り離すことで、1件の異常なメッセージが他の正常なメッセージの処理まで止める事態を防ぎます。
      </p>

      <Heading num="08">データロスが起こりうる3つの瞬間</Heading>
      <p>
        非同期処理は「送ったつもり」が「届いていない」に化けるリスクを常に抱えています。典型的には次の3つの瞬間です。
      </p>
      <Steps>
        <li>送信の失敗 ― 発行したメッセージがキューに届かない。あるいは届いても、受信側が取得する前にブローカー自体がクラッシュする</li>
        <li>取得後・処理前のクラッシュ ― キューからメッセージを取得した後、実際に処理する前にクラッシュする</li>
        <li>永続化の失敗 ― 処理自体は完了したが、その結果をデータベースへ永続化できない</li>
      </Steps>
      <p>
        いずれも、非同期通信では「送信・取得・処理・永続化」が1つのアトミックな操作として保証されないために起こります。
      </p>

      <table>
        <thead>
          <tr><th>対策</th><th>仕組み</th></tr>
        </thead>
        <tbody>
          <tr>
            <td className="hl">永続化と同期送信</td>
            <td>ブローカーがイベントを物理的なデータストアへ永続化した応答を受け取るまで、送信側は次のイベントを送らない。ブローカーが復旧すれば処理を再開できる</td>
          </tr>
          <tr>
            <td className="hl">クライアント確認応答</td>
            <td>処理完了の合図(ACK)を返すまでメッセージをキューから削除しない。処理中にクラッシュしてもメッセージは失われない</td>
          </tr>
          <tr>
            <td className="hl">コミットとの連動</td>
            <td>DBコミットが成功して初めてキューから該当イベントを削除する。コミットと削除の間で取りこぼしが起こらない</td>
          </tr>
        </tbody>
      </table>

      <Heading num="09">非同期の中に擬似的な同期を作る</Heading>
      <p>
        呼び出し元がその場で結果を必要とする場面では、<Term>擬似同期通信</Term>を使います。リクエストとなるイベントに一意の<Term>相関ID</Term>を付けて送信し、応答専用の一時キューで、その相関IDと一致する応答イベントの到着を待ちます。完全にブロックするのではなくタイムアウトを設定し、時間内に来なければタイムアウトエラーとして扱います。
      </p>

      <Heading num="10">データトポロジー</Heading>
      <table>
        <thead>
          <tr><th>トポロジー</th><th>特徴</th></tr>
        </thead>
        <tbody>
          <tr>
            <td className="hl">モノリシックDB</td>
            <td>すべてのプロセッサーが1つの共有DBを使う。実装は単純だが、スキーマ変更が他へ影響し、疎結合の恩恵を打ち消す</td>
          </tr>
          <tr>
            <td className="hl">ドメインDB</td>
            <td>関連するプロセッサー群をドメイン単位でまとめ、ドメインごとに専用DBを持たせる。同一ドメイン内では結合が残る</td>
          </tr>
          <tr>
            <td className="hl">専用DB</td>
            <td>プロセッサーごとに専用DBを持つ。結合は最も弱いが、またがる整合性には<Link href="/database/distributed-transactions">SagaやTCC</Link>が必要になる</td>
          </tr>
        </tbody>
      </table>

      <Heading num="11">リクエストベースと、どう使い分けるか</Heading>
      <table>
        <thead>
          <tr><th>モデル</th><th>メリット</th><th>デメリット</th></tr>
        </thead>
        <tbody>
          <tr>
            <td className="hl">リクエストベース</td>
            <td>処理順序や状態が追いやすく、エラー処理の見通しがよい。結果をその場で返せる</td>
            <td>呼び出し元と呼び出し先が密結合になりやすく、障害や遅延がそのまま伝播する</td>
          </tr>
          <tr>
            <td className="hl">イベントベース</td>
            <td>発行側と購読側が疎結合になり、購読者の追加も容易。スケーラビリティと障害分離に優れる</td>
            <td>業務フロー全体が見えにくく、デバッグや監査が難しい。連鎖が無秩序に広がるリスクがある</td>
          </tr>
        </tbody>
      </table>
      <p>
        実際のシステムでは、即時性が求められる箇所はリクエストベース、疎結合や非同期処理が有効な箇所はイベントベースというように、両者を併用するハイブリッドな設計が一般的です。
      </p>

      <CardGrid>
        <Card><CardNumber>1</CardNumber><h4>イベントは事実の通知</h4><p>応答を期待せず、誰が受け取るかを発行者は知らない。</p></Card>
        <Card><CardNumber>2</CardNumber><h4>非同期・ブロードキャストで疎結合に</h4><p>受信側の処理を待たず、複数の購読者へ同時配信できる。</p></Card>
        <Card><CardNumber>3</CardNumber><h4>連鎖と欠落に注意</h4><p>制御のない派生連鎖と、送信・取得・永続化の各段での取りこぼしが弱点。</p></Card>
      </CardGrid>

      <DocsFooter href="/design/architecture-event-driven" />
    </DocsPage>
  );
}
