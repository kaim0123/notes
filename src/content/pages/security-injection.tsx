import Link from "next/link";
import type { Metadata } from "next";
import {
  DocsPage,
  Hero,
  Eyebrow,
  Lead,
  Term,
  Heading,
  DiagramFrame,
  Card,
  CardGrid,
  CardNumber,
  Analogy,
  Aside,
  DocsFooter,
} from "@/components/docs";

export const metadata: Metadata = { title: "インジェクション攻撃" };

export default function Page() {
  return (
    <DocsPage>
      <Hero>
        <Eyebrow>セキュリティ</Eyebrow>
        <h1>インジェクション攻撃 ― 入力が「命令」として解釈される</h1>
        <Lead>
          <Link href="/security/attacks">攻撃手法</Link>で、Webの脆弱性の多くは同じ形をしていると書きました。<strong>データとして扱うつもりだった入力が、命令として解釈される</strong> ― この一点です。名前は5つも6つもありますが、覚えるべき型は1つしかありません。ここでは共通の構造と、根本的な直し方(値を命令から切り離す)を、SQLインジェクションを軸に見ます。
        </Lead>
      </Hero>

      <Heading num="01">名前は5つでも、起きていることは1つ</Heading>
      <p>
        次の脆弱性はそれぞれ別の名前を持ち、狙われる場所も違います。ただし<Term>攻撃者が用意した文字列を、値として扱われるべき場所に紛れ込ませ、命令として実行させる</Term>という構造は完全に共通です。
      </p>

      <DiagramFrame
        slug="security-injection-surfaces"
        aspect="760 / 300"
        caption="1つの入力文字列が、渡された先ごとに別の名前の脆弱性になる。HTMLへ出せばXSS、SQL文に組み込めばSQLインジェクション、シェルに渡せばOSコマンドインジェクション、レスポンスヘッダに入れればHTTPヘッダインジェクション、メール送信に渡せばメールヘッダインジェクション。狙われる場所は違っても、データとして受け取ったものが命令として解釈された、という一点は同じ。"
      />

      <table>
        <thead>
          <tr><th>脆弱性名</th><th>混ぜ込まれる先</th><th>できてしまうこと</th></tr>
        </thead>
        <tbody>
          <tr><td className="hl">クロスサイトスクリプティング(XSS)</td><td>HTML</td><td>他人のブラウザでスクリプトを実行する</td></tr>
          <tr><td className="hl">SQLインジェクション</td><td>SQL文</td><td>データベースへの命令を書き換える</td></tr>
          <tr><td className="hl">OSコマンドインジェクション</td><td>シェル</td><td>サーバー上で任意のコマンドを実行する</td></tr>
          <tr><td className="hl">HTTPヘッダインジェクション</td><td>レスポンスヘッダ</td><td>ヘッダを追加する・本文を差し替える</td></tr>
          <tr><td className="hl">メールヘッダインジェクション</td><td>メール送信処理</td><td>宛先を追加して迷惑メールの踏み台にする</td></tr>
        </tbody>
      </table>

      <p>
        この見方の利点は、<strong>知らない名前の脆弱性に出会っても、確かめるべきことが決まる</strong>ことです。「外から来た値が、どこかで構文の一部として解釈されていないか」 ― 問いはいつもこれだけになります。
      </p>

      <Heading num="02">SQLインジェクション ― 値が条件を書き換える</Heading>
      <p>
        いちばん被害の大きい形を具体的に見ます。ログインフォームで、入力されたユーザー名を文字列として連結し、SQL文を組み立てているとします。ここに <code>{"' OR '1'='1"}</code> という値が送られると、何が起きるか。
      </p>

      <DiagramFrame
        slug="security-injection-sql"
        aspect="760 / 320"
        caption="文字列連結とプレースホルダの比較。連結した場合、入力に含まれるクォートが値の枠を閉じてしまい、続くORが条件そのものになる。名前が空文字、または常に真という条件になり、全ユーザーの行が一致してパスワードを知らなくても通る。プレースホルダの場合、SQL文の形は疑問符の時点で確定してから値が別便で渡るため、同じ文字列を送ってもその全体が1つの値のままになる。"
      />

      <p>
        違いは<Term>構文が決まる順序</Term>にあります。連結の場合、値を混ぜたあとの文字列をデータベースが解釈するので、値の中の記号にも構文としての意味が与えられます。プレースホルダの場合は、構文が確定したあとに値が届くので、その値がどんな形をしていても構文には触れません。
      </p>

      <Analogy label="💡 たとえるなら">
        プレースホルダは「決まった様式の申込用紙の、名前欄だけを埋めてもらう」やり方です。名前欄に何を書かれても、それは常に名前として扱われます。文字列連結は「白紙の便箋に、申込内容ごと自由に書いてもらう」やり方で、途中に「ここから先は取り消し」と書き足されれば、用紙のルールごと書き換えられます。
      </Analogy>

      <Aside label="対策として弱いもの">
        危ない文字を探して消す(<Term>ブラックリスト</Term>)、クォートをエスケープして自前で連結する、といった対策は、抜け道が必ず残ります。文字コードの違い、想定していない記法、複数の解釈が可能な入力 ― 攻撃側は形を変えるだけで条件を外せます。<strong>「危険な入力を見つける」ではなく「値が構文に混ざれない渡し方に変える」</strong>が唯一の根本対策です。
      </Aside>

      <Heading num="03">3つの段階と、3つの防御</Heading>
      <p>
        とはいえ、プレースホルダで全部が片づくわけではありません。インジェクションは3つの段階を経て起きるので、防御も3段に対応させます。
      </p>

      <DiagramFrame
        slug="security-injection-flow"
        aspect="760 / 300"
        caption="インジェクションが通る3段階と、それぞれに効く防御。入力の段階には検証と正規化、命令を組み立てる段階にはパラメータ化と安全なAPI、実行・表示の段階にはエスケープが対応する。3つは互いの代わりにならず、本命は組み立ての段階 ― 入力検証と出力エスケープは、値として渡せない場面を埋める役割になる。"
      />

      <p>
        重ねる理由は、<strong>②が使えない場所があるから</strong>です。プレースホルダで渡せるのは値だけで、列名や並び順は渡せません。HTMLへの埋め込みにはそもそもプレースホルダの仕組みがなく、出力時のエスケープに頼ります。②で塞げない範囲を、①と③が引き受けるという分担です。
      </p>

      <Heading num="04">入力検証は、サーバー側で・許可リストで</Heading>
      <p>
        入力検証で最初に押さえるのは、<Term>クライアント側の検証はセキュリティ対策ではない</Term>ことです。フォームに「半角英数字のみ」のチェックを入れても、攻撃者はブラウザを使わずにリクエストを直接送れます。クライアント側の検証は入力ミスを減らすための親切、サーバー側の検証が実際の防御 ― 役割がまったく違います。
      </p>

      <table>
        <thead>
          <tr><th>手法</th><th>内容</th></tr>
        </thead>
        <tbody>
          <tr><td className="hl">許可文字の限定</td><td>「英数字とハイフンのみ」のように、許すものを決めて、それ以外を拒否する</td></tr>
          <tr><td className="hl">形式の検証</td><td>数値・日付・メールアドレスなど、型と形式が期待どおりかを確かめる</td></tr>
          <tr><td className="hl">長さの制限</td><td>想定外に長い入力を受け付けない</td></tr>
          <tr><td className="hl">制御文字の除去</td><td>改行やNULL文字など、処理に予期しない影響を与える文字を落とす</td></tr>
        </tbody>
      </table>

      <p>
        ここでも<strong>許可リストで書く</strong>のが原則です。禁止したいものを列挙する方式は、書き漏らしたものが通ってしまう側に倒れます。実装の型としては<Link href="/backend/express-validation">バリデーション</Link>で扱っているように、境界で1回まとめて検証し、そこから先は検証済みの値として扱うのが扱いやすい形です。
      </p>

      <Heading num="05">それでも残るところ ― 列名・並び順・最小権限</Heading>
      <p>
        プレースホルダに渡せないもの ― 動的な列名、<code>ORDER BY</code> の並び順、テーブル名 ― は、<Term>あらかじめ用意した候補の中から選ばせる</Term>形にします。利用者から来た文字列をそのまま列名に使うのではなく、「その文字列が候補リストに含まれるか」を確かめ、含まれていれば<strong>リスト側の値</strong>を使う。これなら、外から来た文字列がSQL文に届くことがありません。
      </p>
      <p>
        最後に置くのが<Term>DBユーザーの最小権限</Term>です。他のすべてを突破されたときに残る一枚で、アプリが使うアカウントにテーブル削除やスキーマ変更の権限が無ければ、被害は閲覧の範囲にとどまる可能性があります。SQL自体の書き方は<Link href="/database/sql">SQL</Link>、接続とクエリの実装は<Link href="/backend/express-database">データベース連携</Link>を参照してください。
      </p>

      <Heading num="まとめ">値を、命令から切り離す</Heading>
      <CardGrid>
        <Card>
          <CardNumber>1</CardNumber>
          <h4>型は1つ</h4>
          <p>名前が違っても、起きているのは「データが命令として解釈された」こと。確かめる問いはいつも同じ。</p>
        </Card>
        <Card>
          <CardNumber>2</CardNumber>
          <h4>探して消すのではなく、混ざらない渡し方に</h4>
          <p>危ない文字を弾く対策は抜け道が残る。構文が確定してから値を渡す仕組みに載せ替える。</p>
        </Card>
        <Card>
          <CardNumber>3</CardNumber>
          <h4>3段で重ねる</h4>
          <p>検証・パラメータ化・エスケープ。②で塞げない列名や画面出力を、①と③が引き受ける。</p>
        </Card>
      </CardGrid>

      <p>
        次は、この型がHTMLの世界で起きたときの姿 ― <Link href="/security/xss">XSSと出力エスケープ</Link>を見ます。
      </p>

      <DocsFooter href="/security/injection" />
    </DocsPage>
  );
}
