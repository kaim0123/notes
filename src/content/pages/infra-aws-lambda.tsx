import Link from "next/link";
import type { Metadata } from "next";
import {
  DocsPage,
  Hero,
  Eyebrow,
  Lead,
  Term,
  Heading,
  DiagramFrame,
  Card,
  CardGrid,
  CardNumber,
  Analogy,
  Aside,
  DocsFooter,
} from "@/components/docs";

export const metadata: Metadata = { title: "Lambda" };

export default function Page() {
  return (
    <DocsPage>
      <Hero>
        <Eyebrow>インフラ・クラウド・運用</Eyebrow>
        <h1>Lambda ― 起きたときだけ動く</h1>
        <Lead>
          <Link href="/infra/aws-compute">コンピューティング</Link>で、Lambdaは管理範囲をいちばん小さくした選択肢だと見ました。ここではその内側 ― <Term>実行環境が用意され、使い回され、片付けられる</Term>という一連の動きを扱います。この仕組みを知らないまま書くと、2回目以降だけ速い、たまに遅い、同じ処理が二度走る、といった現象に振り回されます。逆に知っていれば、どれも設計で織り込めます。
        </Lead>
      </Hero>

      <Heading num="01">実行環境は使い回される</Heading>
      <p>
        呼び出しのたびに、必要なら新しい実行環境が用意されます。しばらく呼ばれていない関数を呼ぶと、環境の用意と初期化の分だけ応答が遅れます ― これが<Term>コールドスタート</Term>です。続けて呼ばれる間は同じ環境が再利用され(<Term>ウォームスタート</Term>)、初期化を飛ばして本体だけが走ります。
      </p>

      <DiagramFrame
        slug="infra-aws-lambda-lifecycle"
        aspect="760 / 300"
        caption="実行環境が用意されてから再利用されるまでの流れ。1回目は環境の用意とコードの初期化が先に走るため応答が遅れる。2回目以降は同じ環境が再利用され、初期化を飛ばして本体だけが動くので速い。したがって重い準備を関数の外側に置けば2回目以降は丸ごと飛ばせるが、その接続は環境が生きている間ずっと残る前提で作る必要がある。しばらく呼ばれないと環境は片付けられ、次はまた初期化から始まる。"
      />

      <p>
        設計上の含意は2つあります。ひとつは<strong>重い準備は関数の外側で1回だけ行う</strong> ― 接続の確立や設定の読み込みをハンドラの中に書くと、毎回のコストになります。もうひとつはその裏返しで、<strong>外側に置いたものは実行環境の寿命の間ずっと残ります</strong>。前の呼び出しの値がグローバル変数に残っていて、別の利用者の処理に混ざる ― という事故はここから起きます。<strong>状態を持ち越さない</strong>ことを前提に書きます。
      </p>

      <Heading num="02">呼び出され方で、失敗の扱いが変わる</Heading>
      <p>
        同じ関数でも、何から呼ばれるかで再試行の仕組みが変わります。ここを取り違えると、失敗が黙って消えたり、逆に何度も実行されたりします。
      </p>

      <DiagramFrame
        slug="infra-aws-lambda-invocation"
        aspect="700 / 300"
        caption="3つの呼び出され方と、失敗したときの扱いの違い。同期の呼び出しでは呼んだ側がその場で結果を待つため、後始末は呼んだ側の責任になる。非同期ではサービス側が自動で再試行し、それでも駄目なものは退避用のキューへ送る。ポーリングではサービス側がキューやストリームを見張ってまとめて渡し、成功したときだけ元から取り除く。どの形でも同じものが二度届きうるため、処理は二度来ても壊れない形にしておく。"
      />

      <table>
        <thead>
          <tr><th>形</th><th>呼び出し元の例</th><th>失敗したら</th></tr>
        </thead>
        <tbody>
          <tr><td className="hl">同期</td><td>HTTPの入口から</td><td>呼び出し元にエラーが返る。再試行は呼び出し元の仕事</td></tr>
          <tr><td className="hl">非同期</td><td>ファイル追加、通知</td><td>サービス側が数回再試行し、駄目なら退避用のキューへ</td></tr>
          <tr><td className="hl">ポーリング</td><td>キュー、ストリーム</td><td>取り除かれないので、また配られる</td></tr>
        </tbody>
      </table>

      <Aside label="二度来ても壊れないように書く">
        どの形でも、同じメッセージが二度届くことがあります(<Link href="/backend/jobs">非同期処理とジョブ</Link>で扱う「少なくとも一度は届く」という性質です)。だから処理は<strong>同じ入力で二度実行しても結果が変わらない</strong>形にします ― 実務では、処理済みのIDを記録して重複を弾く、更新を条件付きにする、といった手当てになります。
      </Aside>

      <Heading num="03">メモリ設定は、CPUの設定でもある</Heading>
      <p>
        直接指定できるのは主に<Term>メモリサイズ</Term>ですが、CPUの割り当てもこれに比例します。つまり<strong>メモリを増やすと処理そのものが速くなる</strong>ので、計算が重い関数では「単価は上がるが実行時間が縮み、総額は同じか安くなる」ことが珍しくありません。
      </p>
      <p>
        逆に言えば、メモリを絞りすぎた関数は<strong>遅いうえに高い</strong>という最悪の位置に来ます。実測して山を探すのが正解で、勘で最小値に張り付けるのは避けます。
      </p>

      <Heading num="04">同時実行数という上限</Heading>
      <p>
        大量のイベントが一度に来れば、その分だけ実行環境が並行して立ち上がります。ただしアカウント単位で<Term>同時実行数</Term>の上限があり、超えた分は拒否されます。ここで2つの問題が起きます。
      </p>
      <ul>
        <li><strong>他の関数を巻き添えにする</strong> ― 上限はアカウントで共有されるので、1つの関数の暴走が無関係な関数まで止めます。重要な関数には個別に枠を予約します。</li>
        <li><strong>下流を押し潰す</strong> ― Lambdaは自動で並列に増えますが、その先のデータベースは増えません。接続数の上限を超えて、データベース側が先に倒れます。関数側の同時実行を意図的に絞る、あるいは<Link href="/backend/data-pool">接続の集約</Link>を挟む、という手当てが要ります。</li>
      </ul>
      <p>
        起動の遅れを避けたい重要な関数には、あらかじめ実行環境を用意しておく設定もあります。ただし常時課金になるので、<strong>本当に遅れが許されないのか</strong>を確かめてから使います。
      </p>

      <Analogy label="💡 たとえるなら">
        必要なときだけ開く屋台です。客が来るたびに組み立てて調理し、途切れればしまう。しばらく誰も来ていなければ組み立てから始まるので最初の1杯は遅く(コールドスタート)、続けて来る間は組んだままなのですぐ出せます。ただし屋台をいくら増やせても、<strong>仕入れ先の店(データベース)は1つ</strong>です。
      </Analogy>

      <Heading num="まとめ">3つを設計に織り込む</Heading>
      <CardGrid>
        <Card>
          <CardNumber>1</CardNumber>
          <h4>環境は使い回される</h4>
          <p>重い準備は外側へ、状態は持ち越さない。速さと事故は同じ性質から出てくる。</p>
        </Card>
        <Card>
          <CardNumber>2</CardNumber>
          <h4>呼ばれ方で再試行が変わる</h4>
          <p>誰が後始末をするのかが形ごとに違う。二度来ても壊れない処理にしておく。</p>
        </Card>
        <Card>
          <CardNumber>3</CardNumber>
          <h4>並列に増えるのは自分だけ</h4>
          <p>下流は同じようには増えない。上限は自分を守るためだけでなく、相手を守るためにも使う。</p>
        </Card>
      </CardGrid>

      <DocsFooter href="/infra/aws-lambda" />
    </DocsPage>
  );
}
